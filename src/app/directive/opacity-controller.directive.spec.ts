import { OpacityControllerDirective } from './opacity-controller.directive';

describe('OpacityControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new OpacityControllerDirective();
    expect(directive).toBeTruthy();
  });
});
